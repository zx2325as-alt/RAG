# ==============================================================
# RAG 线上部署专用环境依赖 (基于 PyTorch 2.1.2 + CUDA 11.8)
# ==========================================

# --- 基础与 Web 框架 ---
flask==3.1.3
flask-sqlalchemy==3.1.1
pymysql==1.1.2
redis==7.3.0
requests==2.32.5
numpy==1.26.4
pandas==2.3.3
gunicorn==25.1.0

# --- RAG 检索与解析组件 ---
faiss-cpu==1.13.2
rapidocr-onnxruntime==1.4.4
pymupdf==1.27.2.2
pdfplumber==0.11.9
python-docx==1.2.0
openpyxl==3.1.5
rank_bm25==0.2.2

# --- LangChain 系列 ---
langchain==1.2.13
langchain-community==0.4.1
langchain-huggingface==1.2.1
langchain-openai==1.1.12

# --- 图数据库 ---
neo4j==6.1.0

# --- 微调与大模型核心组件 ---
# AutoDL 环境内置了 torch==2.4.0+cu118，但如果是 2.1.2 镜像，请保持平台自带，不要在要求文件中强制覆盖
# torch
transformers==5.3.0
tensorboard==2.17.0
llamafactory==0.8.3

# ==============================================================
# vLLM 兼容性安装说明
# ==============================================================
# 注意：vLLM 对 PyTorch 和 CUDA 版本有严格的对应关系。
# 您的环境：PyTorch 2.1.2 + CUDA 11.8 + Python 3.10
# 直接 pip install vllm 会拉取最新版，而最新版通常需要 PyTorch 2.4.0 以上，会导致兼容性报错。
#
# 对于 PyTorch 2.1.2 + CUDA 11.8，最稳定的 vLLM 版本是 0.4.0 或 0.4.1。
vllm==0.4.1

# 如果 pip 安装 vllm 遇到 triton 编译问题，请执行以下命令预装依赖：
# pip install ninja
# pip install -U xformers --index-url https://download.pytorch.org/whl/cu118
