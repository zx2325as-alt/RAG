# ==============================================================
# RAG 线上部署专用环境依赖 (基于 PyTorch 2.1.2 + CUDA 11.8)
# 【最终稳定版 | 无依赖冲突 | 不升级 CUDA | 不破坏环境】
# ==============================================================

# --- 镜像源配置 (加速下载) ---
-i https://mirrors.aliyun.com/pypi/simple/
--trusted-host mirrors.aliyun.com
--extra-index-url https://pypi.tuna.tsinghua.edu.cn/simple/

# --- 基础与 Web 框架 ---
flask==3.1.3
flask-sqlalchemy==3.1.1
pymysql==1.1.2
redis==7.3.0
requests==2.32.5
numpy==1.26.4
pandas==2.3.3
gunicorn==25.1.0

# --- RAG 检索与解析组件 (启用 GPU 加速) ---
faiss-gpu==1.7.2
rapidocr-onnxruntime==1.4.4
onnxruntime-gpu==1.17.1
pymupdf==1.27.2.2
pdfplumber==0.11.9
python-docx==1.2.0
openpyxl==3.1.5
rank_bm25==0.2.2

# ================================
# ✅ 锁定无冲突版本（核心修复）
# ================================
langchain==0.1.20
langchain-community==0.0.38
langchain-huggingface==0.0.1
langchain-openai==0.1.3
langchain-core==0.1.52
langsmith==0.1.59

# --- 图数据库 ---
neo4j==6.1.0

# --- 微调与大模型核心组件 ---
# 🔥 重要：这里 **绝对不写 torch**，使用平台自带 CUDA11.8 版本
# torch
transformers==4.37.4
tensorboard==2.17.0

# 🔥 重要：llamafactory 必须 --no-deps 安装，不能在这里直接装
# llamafactory==0.8.3

# ==============================================================
# vLLM 兼容性安装说明
# ==============================================================
# 注意：您的环境是 CUDA 11.8。如果直接 pip install vllm==0.4.1，
# 默认会拉取 CUDA 12 的扩展包（如 vllm-nccl-cu12）并在本地编译，导致卡死或报错。
# 
# 解决方案：不要在 requirements_online.txt 中安装 vllm。
# 脚本 start_all_linux.sh 已配置好使用 --no-deps 跳过依赖检查安装预编译包。